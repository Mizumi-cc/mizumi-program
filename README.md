## This is an [Anchor](https://github.com/coral-xyz/anchor) project

Build program executable with the command:
`anchor build`

Deploy to devnet with the command:
`anchor deploy`

Run tests with the command: 
`anchor test`
